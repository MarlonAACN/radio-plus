export const AppRoutes = {
  Base: { path: '/', vars: { foo: 'bar' }, query: {} },
  Fallback: { path: '/', vars: { foo: 'bar' }, query: {} },
  Login: {
    path: '/auth/login',
    vars: { foo: 'bar' },
    query: {},
  },
};
