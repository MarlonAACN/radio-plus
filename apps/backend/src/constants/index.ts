import { LogLevel } from '@/types/misc/LogLevel';

export const LOG_LEVEL: LogLevel = 'log';

export const REFRESH_TOKEN_TTL = 604800000; // one week

export const DEFAULT_MARKET = 'DE';

export const SPOTIFY_TOP_TRACKS_LIMIT = 200;

export const SPOTIFY_TOP_ARTISTS_LIMIT = 100;
